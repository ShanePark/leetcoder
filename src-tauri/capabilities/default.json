{
  "$schema": "../gen/schemas/desktop-capability-schema.json",
  "identifier": "default",
  "description": "Default permissions for the leetcoder main window.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:default", "opener:default"]
}
